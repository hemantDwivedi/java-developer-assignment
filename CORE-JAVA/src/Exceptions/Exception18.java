package Exceptions;

// 18. Write a program to generate SQLException.

import java.sql.*;

public class Exception18 {
    public static void main(String[] args) {
        /*try{String sql = String.format("SELECT original_task_id FROM %s WHERE task_id = '%s' and state='%s' LIMIT 1", TABLE_JOB_STATUS_TRACE_LOG, taskId, State.TASK_STAGING);
            DatabaseMetaData databaseMetaData;
            Connection conn = dataSource.getConnection();
        PreparedStatement preparedStatement = conn.prepareStatement(sql);
        ResultSet resultSet = preparedStatement.executeQuery()
    }
    catch (SQLException e){
        System.out.println(e.getMessage());
    }*/
    }
}